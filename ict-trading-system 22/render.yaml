services:
  - type: web
    name: ict-trade-mission-control
    runtime: python
    plan: free
    rootDir: .
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.10"
      - key: AUTH_DISABLED
        value: "true"
      - key: DEFAULT_MODE
        value: "shadow"
      - key: LIVE_LOCKED
        value: "true"
      - key: MANUAL_APPROVAL
        value: "true"

  - type: worker
    name: ict-pipeline-worker
    runtime: python
    plan: free
    rootDir: .
    buildCommand: pip install -r requirements.txt
    startCommand: python scripts/run_pipeline_worker.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.10"
      - key: DEFAULT_MODE
        value: "shadow"
      - key: LIVE_LOCKED
        value: "true"
      - key: MANUAL_APPROVAL
        value: "true"
